import { Server, Socket } from "socket.io"
import { ResponseFunc } from "../type"

export const updateuser= (io: Server, socket: Socket)=>{
    return async (payload,response:ResponseFunc<unknown>)=>{

    }
}